'use client'
import { useState } from 'react'
const initial={company_name:'',contact_name:'',work_email:'',use_case:'',launch_city:'',estimated_locations:10,timeline:'this_quarter',website:''}
export default function PilotForm(){const [form,setForm]=useState(initial);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [reference,setReference]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const response=await fetch('/api/pilot-leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await response.json();if(!response.ok)setError(data.error??'Submission failed');else setReference(data.reference);setBusy(false)}if(reference)return <div className="card p-8 self-start"><div className="text-4xl mb-3">✓</div><h2 className="font-display text-2xl font-extrabold">Application received</h2><p className="mt-3" style={{color:'var(--text-muted)'}}>We will evaluate geography, task criteria, worker safety and expected volume before proposing a pilot.</p><p className="font-mono text-xs mt-5" style={{color:'var(--text-faint)'}}>Reference {reference}</p></div>;const field='mt-2 w-full rounded-xl px-4 py-3 outline-none';return <form onSubmit={submit} className="card p-6 sm:p-8 grid sm:grid-cols-2 gap-5"><label className="text-sm">Company<input required value={form.company_name} onChange={e=>setForm({...form,company_name:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}/></label><label className="text-sm">Your name<input required value={form.contact_name} onChange={e=>setForm({...form,contact_name:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}/></label><label className="text-sm">Work email<input required type="email" value={form.work_email} onChange={e=>setForm({...form,work_email:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}/></label><label className="text-sm">Launch city<input required value={form.launch_city} onChange={e=>setForm({...form,launch_city:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}/></label><label className="text-sm">Estimated locations<input required type="number" min="1" max="100000" value={form.estimated_locations} onChange={e=>setForm({...form,estimated_locations:Number(e.target.value)})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}/></label><label className="text-sm">Target timeline<select value={form.timeline} onChange={e=>setForm({...form,timeline:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}}><option value="this_month">This month</option><option value="this_quarter">This quarter</option><option value="exploring">Exploring</option></select></label><label className="text-sm sm:col-span-2">What decision should the evidence support?<textarea required minLength={20} rows={5} value={form.use_case} onChange={e=>setForm({...form,use_case:e.target.value})} className={field} style={{background:'var(--bg-subtle)',border:'1px solid var(--border)'}} placeholder="Example: verify weekly shelf availability and current price for three SKUs…"/></label><input tabIndex={-1} autoComplete="off" value={form.website} onChange={e=>setForm({...form,website:e.target.value})} className="hidden" aria-hidden="true"/>{error&&<p className="sm:col-span-2 text-sm" style={{color:'var(--accent)'}}>{error}</p>}<button disabled={busy} className="btn btn-primary sm:col-span-2 py-3.5 disabled:opacity-40">{busy?'Submitting…':'Submit pilot application →'}</button><p className="sm:col-span-2 text-xs text-center" style={{color:'var(--text-faint)'}}>Submitting is not a purchase or coverage guarantee.</p></form>}
